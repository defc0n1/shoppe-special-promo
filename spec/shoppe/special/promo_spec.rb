require 'spec_helper'

describe Shoppe::Special::Promo do
  it 'has a version number' do
    expect(Shoppe::Special::Promo::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
